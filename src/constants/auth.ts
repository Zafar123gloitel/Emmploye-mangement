export const UNAUTHORIZED = "Unauthorized, Invalid Token!";

export const FORBIDDEN = "Forbidden, a Token is Required for Authentication!";

export const ROUTE_NOT_FOUND = "Routes Not Found!";

export const INTERNAL_SERVER_ERROR = "Internal Server Error!";

export const INVALID_CREDENTIAL = "Invalid Credential!";

export const INVALID_COACH = "Invalid Coach!";

export const INVALID_USER = "Invalid User!";

export const INVALID_ADMIN = "Invalid Admin!";

export const INVALID_SUPER_ADMIN = "Invalid Super Admin!";

export const INVALID_ROLE = "Invalid RoleType!";

export const BAD_REQUEST = "Something Went Worng, Bad Request!";

export const INVALID_TOKENS =
  "Access and Refresh Tokens are not generated, yet!";

export const THROTTLING_ERROR =
  "The server is processing too many requests at once and is unable to process your request. Retry the request after some time.";

export const BAD_GATEWAY =
  "This error response means that the server got an invalid response while working as a gateway to get a response needed to handle the request.";

export const SERVICE_UNAVAILABLE =
  "The server is not ready to handle the request. Common causes are a server that is down for maintenance or is overloaded.";

export const GATEWAY_TIMEOUT =
  "This error response is given when the server acts as a gateway and cannot get a timely response.";

/// //////////////////////////////////////////////////

export const OK = "Successfull Response";

export const CREATED = "The request succeeded, and a new resource was created.";

export const ACCEPTED = "The request has been received but not yet acted upon.";
